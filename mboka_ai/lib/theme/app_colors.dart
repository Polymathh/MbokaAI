import 'package:flutter/material.dart';

class AppColors {
  static const Color primarySkyBlue = Colors.black;
  static const Color secondaryBlue = Color(0XF00a6fb);
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color backgroundLightGray = Color(0xF52b788);

  // A slightly darker shade for the hover/pressed state (Primary Button)
  static const Color primaryDarkBlue = Color(0xFF009ACD);
}
